# @dojo/widgets/login-form

A basic login form utilising the `password-input` widget and the `form`. Calls the `onSubmit` callback with username and password when the form is submitted and offers the ability to show a forgotten password link.
